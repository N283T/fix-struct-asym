[project]
name = "fix-struct-asym"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "gemmi>=0.7.0",
    "typer>=0.9.0",
    "rich>=13.0.0",
]

[project.scripts]
find-orphan-chains = "scripts.find_orphan_chains:app"
